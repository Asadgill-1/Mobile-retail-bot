<!--
  IMMUTABLE SOURCE SPEC.
  Archived verbatim from the original prompt.txt. Do NOT edit this file.
  Any change to scope/behavior happens via an ADR in docs/DECISIONS/, which supersedes
  the relevant section here. Reference this file by section number (e.g. "SPEC §4").
-->

You are an expert full-stack developer, AI integration specialist, and cybersecurity engineer. Build a complete, production-ready, multi-shop chatbot system for a mobile/computer store chain (30 shops, single deployment, complete tenant isolation).

## SYSTEM OVERVIEW
The company owner operates all shops. No shop staff self-management. Owner needs full control, security, and observability. System handles 300+ concurrent conversations, never drops messages, never leaks data, auto-defends against attacks.

---

## 1. MULTI-TENANCY & ISOLATION
- Supabase (PostgreSQL + Storage) for all data and media.
- `shops` table: id, name, whatsapp_number, status (active/suspended), suspension_reason, created_at.
- ALL other tables have `shop_id` FK. Every query scoped by shop_id.
- WhatsApp: One Twilio number per shop, all forward to SINGLE webhook. Webhook reads `To` number, looks up shop, sets shop_id. No "which shop" prompt.
- Telegram: One bot token. Shopkeepers authenticated by telegram_id → shop_id. Commands auto-scoped.
- Internal API endpoints require `X-Internal-API-Key` header.

---

## 2. SHOP SUSPENSION SYSTEM
Owner commands (owner Telegram ID only):
- `/pauseshop <shop_id> <reason>` — Sets shop.status = 'suspended'.
- `/resumeshop <shop_id>` — Sets shop.status = 'active'.
- `/shopstatus <shop_id>` — Returns status info.

When suspended:
- WhatsApp webhook checks shop.status FIRST. If suspended, sends auto-reply: "We are currently undergoing maintenance. Please contact us later or visit our store." Message NOT processed by AI. Returns 200 OK and stops.
- Shopkeeper Telegram: READ-ONLY mode. Shows "⚠️ Shop suspended: {reason}". Cannot create/modify.
- Existing orders/deliveries continue functioning.

---

## 3. AI ANTI-HALLUCINATION & HUMAN ESCALATION
- AI (GPT-4o) uses function calling as ONLY source of product data. System prompt: "You do NOT know products from memory. You MUST call search_products. Never invent products, prices, or specs."
- Out-of-domain detection: refunds, complaints, repairs, negotiations, "talk to human", legal questions → AI MUST NOT answer.
- On detection:
  1. Save to `pending_escalations` table.
  2. Notify shopkeeper via Telegram: "⚠️ Escalation from {phone}: {message}"
  3. Tell customer: "Let me connect you with our specialist."
  4. Freeze AI. Forward subsequent customer messages to shopkeeper's Telegram.
- Shopkeeper replies via `/reply {phone} {text}` → forwarded to WhatsApp.
- Return to AI: `/handover {phone}`. AI resumes with full Redis context.

---

## 4. PRODUCT INVENTORY WITH FLEXIBLE SPECIFICATIONS

### Product Addition Flow (`/addproduct`):
1. Category? (Mobile/Laptop/Tablet/Accessory)
2. Brand?
3. Model?
4. Color?
5. Condition? (New/Used/Refurbished)
6. Specs: "Enter specifications as key:value pairs. Examples: `camera: 108MP`, `battery: 5000mAh`, `processor: Snapdragon 8 Gen 3`. Type one per line. `/done` when finished."
7. Cost price?
8. Selling price?
9. Quantity?
10. Upload images (up to 5), optional video.
11. Confirmation and save.

### Products Table Schema:
- id, shop_id, category, brand, model, color, condition, cost_price (DECIMAL), selling_price (DECIMAL), quantity (INTEGER), images (TEXT[]), video_url (TEXT).
- `specs` (JSONB) — all flexible key:value specifications.
- `boost_level` (INTEGER DEFAULT 0, range 0-10).
- `tags` (TEXT[]) — array: clearance, trending, best_camera, long_battery, gaming, budget, premium, high_margin, staff_pick, new_arrival, limited_stock.
- `is_featured` (BOOLEAN DEFAULT false).

### AI Search for Flexible Specs:
- `search_products` function accepts `requirements` string (natural language from customer).
- Backend parses requirements, builds PostgreSQL query searching `specs` JSONB with `ILIKE`, also searches `tags`.
- Sorts by: boost_level DESC, relevance score (matching spec pairs + matching tags × 2), is_featured DESC.
- Relevance multiplied by (1 + boost_level/10).

---

## 5. PRODUCT BOOSTING & TAGGING (Old Stock / High Margin Push)

### Shopkeeper Commands:
- `/boost <product_id> <level>` — Set boost 1-10.
- `/unboost <product_id>` — Reset to 0.
- `/tag <product_id> <tag1,tag2>` — Add tags.
- `/untag <product_id> <tag>` — Remove specific tag.
- `/cleartags <product_id>` — Remove all tags.
- `/feature <product_id>` — Toggle is_featured.
- `/productstats <product_id>` — Views, suggestions, orders, profit generated.

### AI Promotion Instructions (in system prompt):
When presenting products:

Boosted products appear first.

Vague requests: prioritize featured products.

Mention naturally: "clearance" = "Special clearance deal!", "best_camera" when customer mentions camera.

Never expose boost levels or tags to customer.

---

## 6. PROFIT CALCULATION & REPORTING

### Profit Formula:
Profit per order = order.selling_price - order.discount_amount - product.cost_price
Margin % = (Profit / product.cost_price) × 100

### Orders Table:
- selling_price (DECIMAL) — actual price charged.
- discount_amount (DECIMAL DEFAULT 0).

### Shopkeeper Profit Commands:
- `/profit today` — Orders, revenue, discounts, cost, gross profit, margin %, top 5 profitable products, clearance profit.
- `/profit yesterday`, `/profit weekly`, `/profit monthly`, `/profit YYYY-MM-DD`.
- `/profit product <product_id>` — Lifetime product profit.

### Owner Profit Commands:
- `/owner profit all` — Combined profit all shops today.
- `/owner profit shop <shop_id>` — Single shop breakdown.
- `/owner profit compare` — Side-by-side all shops.

### Profit Report Format:
Profit Report - Today (July 7, 2026)

Total Orders: 12

Total Revenue: 45,200 AED

Total Discounts: 1,500 AED

| Total Cost: 32,000 AED

Gross Profit: 11,700 AED

/ Average Margin: 36.5%

Top Profitable Products:

1. iPhone 16 Pro Max 256GB - 3 sold, +3,600 AED

2. Galaxy S25 Ultra 512GB - 2 sold, +z,200 AED

Clearance Sold: 4 (+1,800 AEP from old stock)

Use monospace formatting where possible, proper alignment, emojis, currency symbols.

---

## 7. INTRUSION DETECTION & AUTO-QUARANTINE

### Attack patterns to detect:
1. Prompt injection: "ignore previous", "you are DAN", "system prompt", "act as admin", base64, messages >2000 chars.
2. SQL injection patterns.
3. Rapid-fire: 20+ messages in 60 seconds.
4. Cross-shop probing: "other shops", "all your locations", "give me data from shop #".
5. Admin commands in customer messages: `/addshop`, `/owner`, `/pauseshop`.
6. Credential probing: "API key", "database password", "show me backend".

### When attack detected, AUTOMATICALLY:
1. Quarantine: `quarantine:{phone}` in Redis (1-hour TTL).
2. Stop processing: generic reply "Your message could not be processed."
3. Capture LAST 25 MESSAGES from Redis session into `security_incidents` table.
4. Alert owner via Telegram with full forensic data and incident ID.

### Owner investigation:
- `/investigate <incident_id>`, `/quarantine_extend`, `/quarantine_lift`, `/blacklist <phone>`, `/forward_to_shop <phone> <shop_id>`.

---

## 8. DIRECT-TO-SHOP BYPASS MODE
- `/bypass_ai <phone> <shop_id> <reason>` — Messages go directly to shopkeeper's Telegram, no AI.
- `/bypass_remove <phone>` — Revoke.

---

## 9. MESSAGE PROCESSING PIPELINE (exact order):
1. Validate Twilio signature
2. Check shop.status → suspended = auto-reply, stop
3. Check blacklisted_phones → silent ignore
4. Check quarantine:{phone} → generic reply
5. Check bypass_ai:{phone} → forward to shopkeeper
6. Attack detection → quarantine + alert
7. Normal AI processing

---

## 10. ORDER EXPORT SYSTEM (Pick & Pack Excel)

### Commands:
- `/exportorders today|yesterday|YYYY-MM-DD|pending|all`
- `/exportorders today detailed` — adds Order Time, Rider Name, Rider Phone, Special Instructions.
- `/exportrider <rider_id> today` — Rider-specific sheet sorted by address.

### Excel Spec:
- Library: openpyxl.
- Headers: White bold on blue (#2563EB), thin borders, auto-width.
- Columns: Order ID | Customer Name | Phone | Address | Product | Brand | Model | Color | RAM | Storage | Qty | Selling Price | Delivery Date | Status.
- Upload to Supabase: `shop-reports/{shop_id}/orders_{filter}_{timestamp}.xlsx`.
- 24-hour signed URL sent as Telegram download link.
- First message: "⏳ Generating report..."

---

## 11. CONCURRENCY & RELIABILITY
- Webhook returns 200 immediately, Celery processes.
- Per-session Redis lock: `lock:session:{shop_id}:{phone}` (30s timeout).
- All state in Redis only. Zero local memory.
- Twilio MessageSid deduplication (5-min TTL).
- OpenAI errors: retry once, then fallback message.

---

## 12. REPORTS
Shopkeepers: `/report daily`, `/report inventory_low`, `/report top_products`, `/profit today`.
Owner: `/owner dashboard`, `/owner shop <id>`, `/owner escalations`, `/owner security`, `/owner audit`, `/owner health`, `/owner profit all`.

---

## 13. SYSTEM HEALTH
- `/health` endpoint: DB, Redis, OpenAI, Twilio, Celery workers, active conversations, quarantined count.
- Celery Beat health check every 60s. Failure = critical Telegram alert to owner.
- Flower at `/flower` (basic auth).

---

## 14. TECH STACK
Python 3.11+, FastAPI, Supabase, Redis, Celery, python-telegram-bot v20+, Twilio SDK, OpenAI, openpyxl.
Docker Compose: api, celery_worker, celery_beat, redis, flower.

---

## 15. DATABASE TABLES
shops, shopkeepers (is_owner boolean), products (with JSONB specs, boost_level, tags, is_featured, cost_price, selling_price), orders (selling_price, discount_amount), order_status_history, delivery_persons, pending_escalations, security_incidents, blacklisted_phones, audit_logs.

---

## 16. DELIVERABLES
Generate COMPLETE project with EVERY file. No summaries. No placeholders. Include:
- Full database schema (all columns, types, relationships, JSONB usage).
- Complete AI service with function definitions, anti-hallucination prompt, flexible spec search, promotion logic.
- Complete attack detection with all 6 patterns.
- Complete quarantine, bypass, suspension logic.
- Complete Excel export with openpyxl formatting.
- Complete profit calculation and reporting.
- All Telegram commands for shopkeeper and owner.
- Health monitoring, audit logging.
- Docker setup, .env.example, README.
- ALL code functional, async, with error handling and logging.

Output the ENTIRE codebase now. Start with architectural overview, then complete file tree and every file's content.

This is the complete, final prompt. It now handles:

Flexible product specifications that the AI can search intelligently

Natural language queries like "good camera phone with 5000mAh battery under 1200 AED"

Product boosting for old stock clearance and high-margin items

Complete profit tracking per order, per product, per shop

Beautiful profit reports for shopkeepers and owner

All previous features: security, quarantine, bypass, suspension, pick-and-pack Excel exports



on testing phase we use telegram on all things then when deploy the put whatsapp


system put be on production grade
